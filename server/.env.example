# ============================================================================
# HANGMAN GAME BACKEND - ENVIRONMENT VARIABLES
# ============================================================================
# Copy this file to .env and update the values for your environment
# ============================================================================

# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration
DB_PATH=./db/hangman.db

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-make-it-long-and-random
JWT_EXPIRES_IN=24h

# Password Hashing
BCRYPT_ROUNDS=10

# CORS Configuration
CLIENT_URL=http://localhost:3000

# Security Settings
SESSION_TIMEOUT=1800000
MAX_LOGIN_ATTEMPTS=5

# Logging
LOG_LEVEL=info
LOG_FILE=./logs/server.log

# ============================================================================
# PRODUCTION NOTES:
# ============================================================================
# 1. Generate a strong JWT_SECRET using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 2. Set NODE_ENV=production for production deployment
# 3. Use environment-specific CLIENT_URL values
# 4. Consider using higher BCRYPT_ROUNDS (12-15) for production
# 5. Set up proper logging and monitoring
# ============================================================================
